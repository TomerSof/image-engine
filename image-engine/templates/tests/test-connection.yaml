apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-engine.fullname" . }}-test-connection"
  labels:
    {{- include "image-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "image-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
