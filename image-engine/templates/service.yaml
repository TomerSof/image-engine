apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-engine.fullname" . }}
  labels:
    {{- include "image-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-engine.selectorLabels" . | nindent 4 }}
